from setuptools import setup


setup(\
        name="LightningMatrixFactorization", \
        packages=["lmf"], \
        version = "0.0 pre-alpha", \
        description = "A simple demo of matrix factorization with PyTorch Lightning" \
    )
